# Keystore properties for signing the release APK
# Update the passwords and alias to match the values you entered when creating `my-release-key.jks`.
# The `storeFile` path is relative to the `app` module, so point to the keystore in the project root.
storeFile=../my-release-key.jks
storePassword=Dheeraj
keyAlias=my-key-alias
keyPassword=Dheeraj
